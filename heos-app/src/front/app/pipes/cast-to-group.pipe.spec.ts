import { CastToGroupPipe } from './cast-to-group.pipe';

describe('CastToGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new CastToGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
